#include "get_relative_filepath.h"

/**
 * Generates a string that represents the relative path of
 * the entry file to compile.
 *
 * @param args_filename The filename string that was passed in
 * to the program.
 */
char* get_relative_filepath(char* args_filename) {
    return NULL;
}