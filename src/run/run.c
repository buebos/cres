#include "run.h"

/**
 * @brief Resolves the dependencies of the specified file
 *
 * @param app
 * @return void
 */
void run(App* app) {
    cres_log(LOG_INFO, "Entry file is: %s", app->params.file_target);
    cres_log(LOG_WARN, "RUN Command not implemented");
}